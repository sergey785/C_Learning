/*
============================================================
📘 Учебный файл: arithmetic_notes.c
Тема: Арифметические операции и формы присваивания
Назначение:
  Используется для изучения и повторения основ арифметики в C.
  Содержит примеры, комментарии и таблицу приоритетов операторов.
============================================================
*/

#include <stdio.h>

int main(void)
{
    /*
    ============================================================
    🧮 Базовые арифметические операции:

      +  — сложение
      -  — вычитание
      *  — умножение
      /  — деление (целое или вещественное)
      %  — остаток от деления (только для целых чисел)

    Пример:
      10 + 3  → 13
      10 - 3  → 7
      10 * 3  → 30
      10 / 3  → 3   (целочисленное деление)
      10 % 3  → 1
    ============================================================
    */

    int a = 10, b = 3;

    /*
    ============================================================
    ✏️ Сокращённые формы присваивания:

      a += b  эквивалентно  a = a + b;
      a -= b  эквивалентно  a = a - b;
      a *= b  эквивалентно  a = a * b;
      a /= b  эквивалентно  a = a / b;
      a %= b  эквивалентно  a = a % b;

    Пример:
      int a = 10;
      a += 5;   // теперь a = 15
      a *= 2;   // теперь a = 30
    ============================================================
    */

    /*
    ============================================================
    ⚙️ Операции с вещественными числами (тип double):

      Для double оператор % недоступен, 
      остальные работают так же, как и для int.

      Пример:
        double x = 5.5, y = 2.0;
        x + y = 7.5
        x / y = 2.75
    ============================================================
    */

    /*
    ============================================================
    🧠 Важно помнить:
      Чтобы деление выполнялось как вещественное,
      хотя бы один операнд должен быть типа double.

      Пример:
        int a = 5, b = 2;
        double r1 = a / b;        // результат = 2.0  (неверно!)
        double r2 = (double)a / b; // результат = 2.5  ✅ правильно
    ============================================================
    */

    /*
    ============================================================
    📊 Таблица приоритетов арифметических операторов

    Приоритет ↓   |  Операторы     |  Описание
    ---------------|----------------|-----------------------------
    1 (высокий)    |  *  /  %       |  умножение, деление, остаток
    2 (ниже)       |  +  -          |  сложение, вычитание
    3 (низкий)     |  =, +=, -= ... |  присваивание

    💡 Пример:
        int a = 2 + 3 * 4;   // сначала 3 * 4 = 12, потом 2 + 12 = 14
        int b = (2 + 3) * 4; // скобки меняют порядок → 5 * 4 = 20

    Правило:
      Операторы с одинаковым приоритетом выполняются
      слева направо (левая ассоциативность).
    ============================================================
    */

    /*
    ============================================================
    ✅ Итог:
      • Арифметика в C строго типизирована.
      • Деление целых чисел отбрасывает дробную часть.
      • Приоритет операторов можно изменить с помощью скобок.
      • Используйте double для точных вычислений.
    ============================================================
    */

    return 0;
}
